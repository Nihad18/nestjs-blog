import { User } from './user.entity';
import { Blog } from './blog.entity';
import { Role } from './role.entity';
import { Tag } from './tag.entity';
const entities = { User, Blog, Role, Tag };

export { User, Blog, Role, Tag };
export default entities;
